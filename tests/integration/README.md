# Integration Tests

Integration tests that require Supabase credentials:
- API endpoint tests (pytest + httpx test client)
- Database connection tests
- RLS policy verification
- Migration application tests
