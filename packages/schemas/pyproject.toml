[project]
name = "chromagora-schemas"
version = "0.1.0"
description = "Shared schemas for Chromagora OS"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
