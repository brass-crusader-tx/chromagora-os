[project]
name = "chromagora-config"
version = "0.1.0"
description = "Shared configuration for Chromagora OS"
requires-python = ">=3.11"
dependencies = [
    "pydantic-settings>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
