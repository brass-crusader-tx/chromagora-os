# launchd Services

Active Chromagora OS services:

- `com.chromagora.api.plist` — FastAPI backend
- `com.chromagora.os-web.plist` — Next.js cockpit

Archived carryover services live in `archive/` and are not part of the current Chromagora OS runtime.
